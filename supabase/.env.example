# Local Supabase CLI values only. Never commit real credentials.
# The hosted development project is identified in config.toml; linking and
# remote migration application require an explicit, separately approved step.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PUBLISHABLE_KEY=
